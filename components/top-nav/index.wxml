<view class="top-nav">
  <view wx:for="{{tabList}}" wx:key="item" data-index="{{index}}" bindtap="navTap"
        class="top-nav-tab {{index === currentTab ? 'active' : ''}}">
    <view class="top-nav-label">{{item}}</view>
  </view>
</view>
